# Dipakai oleh api/zae-chat.js (serverless function), BUKAN oleh frontend.
# Ambil gratis di: https://aistudio.google.com/apikey
# Di Vercel: Project Settings → Environment Variables → tambahkan ini, lalu redeploy.
# Untuk dev lokal dengan `vercel dev`: copy file ini jadi `.env.local` dan isi key-nya.
GEMINI_API_KEY=
